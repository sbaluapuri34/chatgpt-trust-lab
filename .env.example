# Phase 1 uses Playwright (no Reddit API keys required for collection).
# Phase 1 is LOCKED — see project_state.yaml and docs/PHASE_1_CHECKPOINT.md

# Phase 3 LLM keys (Groq primary, Gemini fallback):
GROQ_API_KEY=
GEMINI_API_KEY=

# Dry run without keys:
#   python -m phase3 --mock
